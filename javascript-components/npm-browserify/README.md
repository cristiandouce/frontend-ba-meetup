# Javascript component - NPM + Browserify example

## Run

```bash
npm install

mkdir build

browserify -r index.js:multiply-by-three -o build/build.js

```

and open the `example.html` file on this directory.
